<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="CashFlow.Views.AtivoFinanceiroDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CashFlow.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Style="{StaticResource DefaultContentDialogStyle}"
    Margin="0"
    Padding="0"
    x:Name="AtivoFinanceiro"
    Title="Ativo Financeiro"
    Closing="AtivoFinanceiro_Closing"
    PrimaryButtonText="OK"
    PrimaryButtonClick="AtivoFinanceiro_PrimaryButtonClick"
    Background="{StaticResource Cinza13}">

    <Grid Margin="0" Padding="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Padding="0" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="8" Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox HorizontalAlignment="Stretch" MinWidth="260" Width="260" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Orientation="Horizontal" Margin="0" Spacing="4">
                    <Button x:Name="btnExcluir" Width="34" Height="34" Padding="0" Margin="0" Style="{StaticResource AlternateCloseButtonStyle}">
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE74D;"/>
                    </Button>
                    <Button x:Name="btnEditar" Width="34" Height="34" Padding="0" Margin="0" Style="{StaticResource AlternateCloseButtonStyle}">
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE70F;"/>
                    </Button>
                    <Button x:Name="btnAdicionar" Width="34" Height="34" Padding="0" Margin="0" Style="{StaticResource DefaultButtonStyle}">
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE710;"/>
                    </Button>
                </StackPanel>
            </Grid>

            <StackPanel Spacing="8" Orientation="Vertical" Grid.Row="1" Background="{StaticResource Cinza11}" Margin="0" Padding="12" CornerRadius="4">
                <TextBlock Text="Nome" FontSize="16" FontWeight="Normal"/>
                <TextBox Text="" FontSize="16" FontWeight="Normal"/>
            </StackPanel>
        </Grid>
    </Grid>
</ContentDialog>
