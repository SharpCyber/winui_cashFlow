<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="CashFlow.Views.AtivoRegistroDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CashFlow.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="AtivoFinanceiro"
    Style="{StaticResource DefaultContentDialogStyle}"
    PrimaryButtonClick="AtivoFinanceiro_PrimaryButtonClick"
    PrimaryButtonText="Fechar"
    Margin="0"
    Padding="0"
    Closing="AtivoFinanceiro_Closing"
    Loaded="AtivoFinanceiro_Loaded"
    Background="{StaticResource Cinza13}">

    <Grid Margin="0,0,0,-15" Padding="0" MinWidth="500" MinHeight="280">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Padding="2,0" RowSpacing="12" Margin="0" HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Spacing="8">
                <Grid HorizontalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Ativo Financeiro" 
                           VerticalAlignment="Center" 
                           FontFamily="Arial" 
                           FontSize="24" 
                           FontWeight="Bold"/>

                    <Button x:Name="btnAjuda" 
                        Grid.Column="1"
                        Click="btnAjuda_Click" 
                        Style="{StaticResource DefaultButtonStyleAjuda_28x}">
                    </Button>
                </Grid>

                <StackPanel x:Name="spAjuda" Grid.Row="1" Visibility="Collapsed" HorizontalAlignment="Stretch" Padding="6,0"/>

                <TextBox x:Name="txtPesquisarAtivo"
                         HorizontalAlignment="Stretch"
                         PlaceholderText="Pesquisar ativo"
                         Margin="0"
                         Text=""
                         MaxLength="50"
                         KeyUp="txtPesquisarAtivo_KeyUp"
                         Foreground="{StaticResource Branco}" 
                         FontWeight="Normal"/>

            </StackPanel>

            <StackPanel Spacing="8" Orientation="Vertical" Grid.Row="1" Background="{StaticResource Nenhuma}" Margin="0" Padding="2" CornerRadius="0">
                <ScrollViewer Grid.Row="2" x:Name="scroll" Margin="0" LayoutUpdated="scroll_LayoutUpdated">
                    <ItemsControl x:Name="spPrincipal" ItemsSource="{Binding Items}" Margin="0">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Background="{StaticResource Cinza12}" CornerRadius="8" Padding="6" Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="{Binding Codigo}" 
                                               VerticalAlignment="Center" 
                                               Margin="6,0"
                                               Padding="2"
                                               Foreground="{StaticResource Branco}" />

                                    <Button x:Name="btnAdicionar" 
                                            Click="btnAdicionar_Click" 
                                            Grid.Column="1"
                                            Visibility="{Binding ExibirBotaoCrud}" 
                                            Style="{StaticResource AccentButtonStyle_34x}">
                                        <FontIcon Style="{StaticResource FontIcon_16}" Glyph="&#xE710;"/>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>

            <StackPanel x:Name="spAviso" Grid.Row="2" HorizontalAlignment="Stretch" Padding="6,0"/>
        </Grid>
    </Grid>
</ContentDialog>
