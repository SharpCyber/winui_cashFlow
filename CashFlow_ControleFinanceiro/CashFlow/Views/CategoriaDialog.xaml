<ContentDialog
    x:Class="CashFlow.Views.CategoriaDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CashFlow.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="Categoria"
    Title="Categorias"
    Style="{StaticResource DefaultContentDialogStyle}"
    PrimaryButtonClick="Categoria_PrimaryButtonClick"
    PrimaryButtonText="Fechar"
    Margin="0"
    Padding="0"
    Closing="Categoria_Closing"
    Loaded="Categoria_Loaded"
    Background="{StaticResource Cinza13}">
    
    <Grid Margin="0,0,0,-15" Padding="0" MinWidth="500">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Padding="0" RowSpacing="12" Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="8" Grid.Row="0" Padding="0" Margin="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox x:Name="cboCategoria" 
                          HorizontalAlignment="Stretch" 
                          VerticalAlignment="Center"
                          Margin="0" 
                          Padding="12,0,0,0"    
                          IsEnabled="{Binding MostrarBotoesCrud}"
                          ItemsSource="{Binding Categorias}"
                          SelectedItem="{Binding CategoriaSelecionada, Mode=TwoWay}"
                          DisplayMemberPath="Nome">
                </ComboBox>
            </Grid>

            <StackPanel Spacing="8" Orientation="Vertical" Grid.Row="1" Background="{StaticResource Cinza11}" Margin="0" Padding="18" CornerRadius="4">
                <TextBlock Text="Nome" FontSize="16" FontWeight="Normal"/>
                <TextBox Text="{Binding Nome}"
                         IsEnabled="{Binding EditarNome}"
                         FontSize="16" 
                         FontWeight="Normal" 
                         Margin="0" 
                         MaxLength="100"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" Orientation="Horizontal" Margin="0" Spacing="4">
                    
                    <Button x:Name="btnCancelar" 
                            Click="btnCancelar_Click" 
                            Visibility="{Binding MostrarBotoesSalvarCancelar}" 
                            Style="{StaticResource DefaultButtonStyle_34x}">
                        <FontIcon Style="{StaticResource FontIcon_16}" Glyph="&#xE711;"/>
                    </Button>
                    <Button x:Name="btnSalvar" 
                            Click="btnSalvar_Click" 
                            Visibility="{Binding MostrarBotoesSalvarCancelar}" 
                            Style="{StaticResource AccentButtonStyle_34x}">
                        <FontIcon Style="{StaticResource FontIcon_16}" Glyph="&#xE74E;"/>
                    </Button>
                    <Button x:Name="btnExcluir" 
                            Click="btnExcluir_Click" 
                            Visibility="{Binding MostrarBotoesCrud}" 
                            IsEnabled="{Binding HabilitarBotoesCrud}"
                            Style="{StaticResource DefaultButtonStyle_34x}">
                        <FontIcon Style="{StaticResource FontIcon_16}" Glyph="&#xE74D;"/>
                    </Button>
                    <Button x:Name="btnEditar" 
                            Click="btnEditar_Click" 
                            Visibility="{Binding MostrarBotoesCrud}" 
                            IsEnabled="{Binding HabilitarBotoesCrud}"
                            Style="{StaticResource DefaultButtonStyle_34x}">
                        <FontIcon Style="{StaticResource FontIcon_16}" Glyph="&#xE70F;"/>
                    </Button>
                    <Button x:Name="btnAdicionar" 
                            Click="btnAdicionar_Click" 
                            Visibility="{Binding MostrarBotoesCrud}" 
                            Style="{StaticResource AccentButtonStyle_34x}">
                        <FontIcon Style="{StaticResource FontIcon_16}" Glyph="&#xE710;"/>
                    </Button>
                    
                </StackPanel>
            </StackPanel>

            <StackPanel x:Name="spAviso" Grid.Row="2" HorizontalAlignment="Stretch" Padding="6,0"/>
        </Grid>
    </Grid>
</ContentDialog>